# Simple Express API for User resource

## Guide

 * Clone the repository:
    `git clone git remote add origin https://github.com/belloshehu/3mtt-basic-express-api.git`
   
 * Install packages:
     `npm install`

 * Start the server:
     `npm start`

  
   
    
